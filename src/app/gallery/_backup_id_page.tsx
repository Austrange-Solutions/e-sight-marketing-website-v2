// Backup of previous [id] page — moved to avoid dynamic segment name conflict with [slug].
// This file is inert and not used by the router. Keep as a backup; can be removed later.
export default function _backup() {
  return null;
}
